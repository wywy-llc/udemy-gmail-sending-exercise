/**
 * メールを送信してみよう
 */
function sendEmail1() {
  const recipient = '送信先メールアドレス';
  const subject = 'メール件名';
  const body = 'メール本文'
  Logger.log(`送信先メールアドレス：${recipient}\nメール件名：${subject}\nメール本文：${body}`);
  
  // 演習1-1：Gメールを送信してください。
  
}